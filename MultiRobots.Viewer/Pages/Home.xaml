<UserControl x:Class="MultiRobots.Viewer.Pages.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MultiRobots.Viewer.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920"
              Background="Black">
    <Grid x:Name="myGrid">
        <!--<MediaElement Name="mediaPlayer"
                      Stretch="Fill"
                      UnloadedBehavior="Manual"
                      MediaEnded="mediaElement_OnMediaEnded">
            <MediaElement.Triggers>
                <EventTrigger RoutedEvent="MediaElement.Loaded">
                    <EventTrigger.Actions>
                        <BeginStoryboard>
                            <Storyboard>
                                <MediaTimeline Source="introduction.wmv" 
                                               Storyboard.TargetName="mediaPlayer"
                                               RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger.Actions>
                </EventTrigger>
            </MediaElement.Triggers>
        </MediaElement>-->
    </Grid>
</UserControl>
